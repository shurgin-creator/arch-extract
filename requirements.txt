# Core dependencies for the project
streamlit
# Google Gemini SDK
google-genai
pdf2image
pillow
python-dotenv
pandas
openpyxl
# Hybrid PDF text extraction (Phase 1 accuracy boost)
PyMuPDF
# Cloud persistence and auth (Phase 2/3)
supabase
